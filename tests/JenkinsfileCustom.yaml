// oc policy add-role-to-user edit system:serviceaccount:devops:jenkins -n sandbox
// oc policy add-role-to-user edit system:serviceaccount:devops:jenkins -n sandbox
// oc policy add-role-to-user system:image-puller system:serviceaccount:devops:jenkins -n sandbox

// Helpful resources
// https://hub.docker.com/r/csanchez/jenkins-kubernetes/
// https://itnext.io/running-integration-tests-with-kubernetes-ae0fb71e207b


podTemplate(
  label: 'wingnut-tests',
  cloud: 'openshift', //This needs to match the cloud name in jenkins-kubernetes-plugin config
  yaml: """
apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: some-label-value
spec:
  containers:
  - name: wingnut
    image: image-registry.openshift-image-registry.svc:5000/devops/wingnut:latest
    imagePullPolicy: Always
    resourceLimitMemory: 512Mi
"""
)
{
    node('wingnut-tests') {
        stage('tests') {
          echo "Running tests"
          container('wingnut') {
              sh "python -m pytest"
          }   
        }
    }
}


def label = "wingnut-${UUID.randomUUID().toString()}"
podTemplate(label: label, yaml: """
apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: some-label-value
spec:
  containers:
  - name: wingnut
    image: image-registry.openshift-image-registry.svc:5000/devops/wingnut:latest
    imagePullPolicy: Always
    resourceLimitMemory: 512Mi
"""