node {
  stage('Build Tasks') {
      openshift.withCluster() {
          openshift.withProject("devops") {
              openshift.selector("bc", "wingnut").startBuild("--wait=true")
      }
    }
  }
}